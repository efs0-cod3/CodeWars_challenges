// P -> two number in str form
// R -> sum of the two in str
// E -> "4",  "5" --> "9"
// P -> utilize Math.abs() function to returns the absolute value, 
//      and covert toString() the result
function sumStr(a, b) {
return (Math.abs(a) + Math.abs(b)).toString()
}