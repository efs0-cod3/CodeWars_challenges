export function sortMe (names: string[]): string[] {
    // Your code here
  //   use sort method to "sort" the strings in ascending order
    return names.sort()
  }
  sortMe(["one", "two", "three"])