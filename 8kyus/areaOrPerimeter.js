// 8kyu
const areaOrPerimeter = function(l , w) {
    // Return your answer
    
    if(l === w){
      return l*w
    }else {
      return 2*l + 2*w
    }
  };
  
  //  formulas
  // perimeter => P = 2l + 2w
  // area => A = l*w 